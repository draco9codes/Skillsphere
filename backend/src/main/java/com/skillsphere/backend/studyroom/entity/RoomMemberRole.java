package com.skillsphere.backend.studyroom.entity;

public enum RoomMemberRole {
    CREATOR,     // User who created the room
    MODERATOR,   // Can moderate (future feature)
    MEMBER       // Regular participant
}
